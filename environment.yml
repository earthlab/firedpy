name: firedpy  # Replace with your desired environment name
channels:
  - default
  - conda-forge  # Use the conda-forge channel for some packages
dependencies:
  - gdal
  - geopandas
  - pandas
  - numpy 
  - paramiko
  - rasterio
  - requests
  - netCDF4
  - tqdm
  - beautifulsoup4
  - matplotlib
  - xarray
  - shapely
  - scipy
  - mock
  - libgdal-hdf4
